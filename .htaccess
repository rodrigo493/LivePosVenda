<IfModule mod_rewrite.c>
  RewriteEngine On

  # Redirecionar root para dist/
  RewriteCond %{REQUEST_URI} !^/LivePosVenda/dist/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^(.*)$ dist/$1 [L]
</IfModule>
