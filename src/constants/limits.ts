// Preview rows shown in import dialogs before confirming
export const PREVIEW_ROWS = 5;

// Max columns shown in import preview tables
export const PREVIEW_COLUMNS = 8;

// Items shown in ranking/leaderboard lists
export const RANKING_LIMIT = 10;

// Items shown in compact team/ranking views
export const COMPACT_LIST_LIMIT = 8;

// Items shown in dashboard card lists (quotes, WOs, warranties, SRs)
export const CARD_LIST_LIMIT = 4;

// Suggested parts shown in product search
export const SUGGESTED_PARTS_LIMIT = 12;

// Search results dropdown limit
export const SEARCH_RESULTS_LIMIT = 20;

// Portal page query limits
export const PORTAL_LIST_LIMIT = 20;
export const PORTAL_WARRANTY_LIMIT = 10;

// Activity log entries per page
export const ACTIVITY_LOG_LIMIT = 50;

// Large data fetch limit for dashboards
export const DASHBOARD_QUERY_LIMIT = 200;

// WhatsApp chat message history
export const CHAT_HISTORY_LIMIT = 200;

// Delayed tickets shown in my dashboard
export const DELAYED_LIST_LIMIT = 20;
